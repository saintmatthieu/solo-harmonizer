{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(msvc) Launch",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": ["--gtest_filter=*Poissons*"],
      // "args": ["--gtest_filter=*DefaultMelodyRecognizer*easy"],
      // "args": ["--gtest_filter=*DefaultMelodyTracker*"],
      // "args": ["--gtest_filter=*KrumhanslSchmucklerAlgorithm*"],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
